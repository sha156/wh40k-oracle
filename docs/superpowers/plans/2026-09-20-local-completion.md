# Local Docker completion roadmap

## Latest September 26 local cleanup

The user resumed work and then retired `data/沃坦联盟CODEX-双子星版 V1.30.pdf`. Its PDF/refined cache are in an excluded archive; 73 active chunks were removed (5,754 → 5,681), with every retained document/vector unchanged and SQLite byte-identical. Persistent policy guards block re-ingestion through PDF and derived-cache entry points. Full native suite **2,687 passed**; frontend **22**, type/lint and real Chrome **12/12** passed, including actual download/clipboard delivery. Docker is running with all assets and completed warmup; 117 deployed files match. Historical Calgar identity is repaired, but one of three latest comparisons still uses a lossless formatting fallback. Black Library capture remains partial (98 unresolved). **Removal of all other fan-translated PDFs is awaiting the user's scope answer; only the named PDF is retired.** See [current acceptance](../reports/2026-09-26-local-cleanup-acceptance.md). Earlier paused headings below are historical.


Scope: finish the local Docker application first, per the user's September 20 decision. Cloud deployment is deferred. This audit supersedes the open-status claims in the July remaining-task inventory and earlier release checkpoints; those documents remain historical evidence.

## September 26 source freeze after formatter diagnosis

- [x] Project the retained ordinary-Calgar composition into the archive tool summary, test it, deploy it and verify that seven fresh answers preserve the ordinary/armour identity boundary.
- [x] Diagnose the three comparison-only formatting fallbacks with an exact DeepSeek Flash replay. The structured draft was valid; the exact full-row-label guard rejected natural short Chinese names.
- [x] Add a collision-aware short-name equivalence rule and three regressions. Focused identity/formatter suite: **113 passed**; independent final review approved with zero findings.
- [ ] Rebuild/deploy this final formatter edit and repeat full-prose live acceptance. The last full native run predates this edit (**2,673 passed**, 19 warnings), so final-tree full verification remains open.
- [ ] Verify Markdown download and clipboard delivery in a real browser. The preview content was previously visible, but delivery was not established.
- [ ] Keep Dreadherder preview projection and the other unreleased/public-source gaps deferred until work resumes; the user requested save and stop.

Source freeze details: [formatter short-name fix](../reports/2026-09-26-paused-formatter-short-name-fix.md). No application commit, push or merge is claimed.

## Latest September 26 acceptance, paused before identity projection

- [x] Review and deploy evidence preservation across later misses and step limits; verify actual qualified facts survive in normal and emergency answers.
- [x] Review/deploy narrow generation and formatter grounding guards: 112 focused tests, independent approval, final native **2,662 passed** (19 warnings).
- [x] Correct simulator E2E route/expectation and pass **9/9** on the preceding API image. Verify current image source hashes, assets and warmup.
- [x] Preserve every rejected/mixed response. Latest seven fresh chats have **six passing scoped checks and one rejected comparison**; current image is not fully accepted.
- [ ] Project the already-verified ordinary-Calgar composition into the archive points summary. Read-only diagnosis is complete; code is not written.
- [ ] Review/test that change, rebuild and repeat full-prose live acceptance, then assess app publication/PR readiness. Source stays uncommitted and PR #75 unchanged in this resume.
- [ ] Keep the separate partial crawl deferred unless needed; no new source fetch is required for this defect. Official prices remain the September 14 snapshot.

The coordinated source task received the user's save-and-stop request. Docker remains running on API `23855c9f...` and web `5156e1f5...`. [Resume checkpoint](../reports/2026-09-26-paused-final-docker-checkpoint.md). Earlier statuses below are historical.

## September 26 Docker acceptance, paused by user

The September 22 API source was rebuilt/deployed and all assets/warmup passed. Native suite passed 2,646 before the final evidence-preservation draft; frontend 22 tests/type/lint passed. Live roster overflow, faction/detachment validation, all 27 retired enhancements and paid/free/unknown weapon pricing passed. Three individual Calgar/Guilliman chats passed, but the comparison exposed a later missing alias lookup discarding earlier successful evidence. Its narrow fix is saved with 56 focused tests passing; independent review and deployment remain unfinished. Existing browser suite was 7/8 because one expectation predates client-side invalid-model rejection; the saved replacement test still needs review and a run.

The user requested save and stop. Containers remain running, source changes remain uncommitted, and the separate crawl remains stopped. Resume from the [Docker checkpoint](../reports/2026-09-26-paused-docker-checkpoint.md) and [source checkpoint](../reports/2026-09-26-paused-evidence-preservation-fix.md). API image `c8841e4eaa8c376c8e6cd502b5738707a7aa4cdc802b756f9c975b45f55d6201` does not contain the latest draft loop fix; web remains `5156e1f53e6792b1f3de10da01106b885c5ba36e790c2a50cdaa48b2997cedda`. Full final-tree acceptance, source publication and PR readiness are not claimed.

## September 22 review and capture, paused by user

Historical Calgar lookup and distinct citations are implemented and live-checked after a backed-up archive projection; canonical tables and prices remained unchanged. Review also fixed malformed answer sources, roster faction/removal/pricing checks, invalid counts, simulator named-model compositions and stale frontend requests. Full native suite passed 2,628 before final focused additions; later suites passed 133 (official/historical lookup), 69 (roster pricing/API), 17 (crawler), and 22 frontend tests plus type/lint. Latest web build and selected rapid-change journeys passed; final API deployment/acceptance remains paused.

A separate Black Library snapshot reconciled 1,171 inventory rows, 1,071 full details and 2,514 powers, plus faction/universal-rule catalogues. It is not a complete-site capture: 98 details remain unresolved, two known empty army responses need final-script reclassification, and additional mini-program sections have not been observed. No fresh snapshot was projected into canonical caches/DB. Source changes are saved locally and uncommitted; PR #75 is unmerged. See the [September 22 save checkpoint](../reports/2026-09-22-paused-review-crawl-checkpoint.md) for paths, exact checks and resume order. The September 21 open-status paragraph below is historical.

## September 21 continuation, paused by user

Guilliman short-name lookup, evidence retention, input validation, explicit DeepSeek Flash and the simplified conversation view are implemented. The original Warhammer style has been restored in the running web image after user feedback. Readability/copy refinements are saved locally. These are tracked by draft PR #75 and the [September 21 acceptance report](../reports/2026-09-21-chat-review-acceptance.md), with exact paused state in the [Calgar checkpoint](../reports/2026-09-21-paused-chat-calgar-checkpoint.md).

Next unfinished task: expose deleted historical Black Library cards without mixing them with current units. Ordinary Calgar's old 200-point source record was skipped by the canonical overlay importer; it must remain distinct from the current Armour of Antilochus record at 155. Archive projection/restore code is saved and its 37 focused tests passed; agent lookup, citations, runtime projection and live acceptance remain incomplete. The integration-test draft fails and needs fixture repair. Finish this work, check the final dark mobile layout, then run final review/tests and publish the revised PR. No final-tree CI or merge is claimed.

## Implemented and verified

| Area | Current state and evidence |
| --- | --- |
| Edition migration, DSL and structured authority layers | Existing implementation complete; September reconciliation restores 390 reviewed patches and checks 2,901 DSL fingerprints. This continuation did not rebuild or modify the database. |
| Official points | Existing ledger has 3,893 rows; latest reconciliation matched 1,333 operational tiers and 879 enhancement rows. September 20 live price browsing confirms the ledger and both Warlord prices at 3,500. Cache is dated September 14; this is not a new source fetch. |
| Four local web sections | Chat, codex, simulator and roster laboratory implemented and exercised through Docker. |
| Roster text import, validation and critique | Already implemented. Live two-unit roster parses, validates at 150 points and critiques its equipped unit; an unknown third line remains explicitly unresolved. Corrected stale agent-tool text that claimed parsing was unavailable. |
| Conversation memory | September 18 acceptance passed six same-session recall trials, isolation and fresh-fact checks. No new full memory benchmark this pass. |
| Canonical identity and faction inventory | Preserve IDs across wiki lookup, refuse duplicate IDs, separate pagination from total counts and source prices. Live Titan answer confirms four shared Imperial/Chaos datasheets. |
| Referenced rules and citations | Retrieve army-rule text separately from card references. Web formatting now supplies a dedicated merged-card citation and retains source-scope warnings. Final live command answer lists all six effects and separates PDF effects from card-specific counts/ranges. |
| Lossy answer formatting | Preserve named Markdown table rows or fall back to the original reply with a visible warning. This is a narrow content-loss guard, not general semantic equivalence checking. |
| Keyword-to-core-rule links | Already implemented; live index has 50 entries, 37 with chapter links, and sampled detail/chapter requests succeed. Not every unit-specific keyword has an official section link. |
| Saving an answer | Added user-controlled Markdown export with citations, AI/unreviewed labeling and a lossless JSON snapshot. Download and visible copy-panel controls are provided. No AI answer is automatically promoted into authoritative wiki content. |
| Docker deployment | Both images build and run; required assets and model warmup pass. Final release evidence is in the September 20 acceptance report. |
| README artwork and font delivery | Existing README artwork and self-hosted display font are present. The documented system-font fallback for Chinese remains a deliberate performance choice. |

## Remaining work with explicit limits

1. Acquire complete suitable **public official** rules for Gunwagon, Runtherd and 28 Ork enhancement keys. Keep preview-derived records labeled. Current points do not establish current rules coverage.
2. Establish Docker Desktop restart durability. The user started it successfully this time; the earlier stale-socket recovery is a workaround, not a demonstrated permanent fix. Do not restart the host solely to manufacture a passing claim.
3. Continue rolling AI quality evaluation. The September 18 agent benchmark was 115/115; September 20 checks cover selected final web responses and deterministic regressions, not another full benchmark. Arbitrary sentence-to-source correctness remains probabilistic.
4. Revisit cloud hosting only after the local app, as requested. No cloud server/provider changes were made.
5. Automatic `archive_answer` insertion into the official wiki remains intentionally unwired. The wiki constitution reserves FAQ pages for official FAQ; any AI archive integration needs a separate non-authoritative content design. Manual Markdown export is available now.

Release tracking: [PR #74](https://github.com/sha156/wh40k-oracle/pull/74). Exact tests, browser limits and deployment evidence: [September 20 acceptance](../reports/2026-09-20-lookup-deployment-acceptance.md).
