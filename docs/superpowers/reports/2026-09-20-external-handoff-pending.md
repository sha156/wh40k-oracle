# Prepared knowledge handoff — external publication pending

## Latest September 26 reviewed-fix handoff published

The final coordinated stop is saved in `2026-09-26-paused-final-docker-checkpoint.md`. Current API `23855c9f...` passed 2,662 native tests and runtime source/health checks; the latest seven live probes remain mixed 6/7. The ordinary-Calgar identity projection is diagnosed but not implemented. The earlier evidence-loss fix is independently reviewed and verified live, so its existing learning decision was updated and one completed underlying-error record was added after duplicate inspection. No new placeholder was created for the unresolved projection.

Published note commits: project devlog `73a43a2`, learn-notes `8d81a5c`, error-notes `6788180`. All three pushes succeeded and each repository was verified 0 ahead / 0 behind its upstream. Only the intended note files and their index changes were committed; unrelated staged/unstaged files remain intact. No application commit/push/merge or harness promotion occurred. Earlier publication and rejection entries below are historical.

## September 26 Docker handoff publication completed

The later explicit stop hook completed the Docker-specific handoff through the normal note repositories. Duplicate inspection found the coordinated source checkpoint and raw decision already published as devlog `e4596da` and learn-notes `e941934`. This task added deployed image identities, actual native/frontend/API/browser results and the unresolved draft-test route issue to the same devlog, published as `6a708f9`. The existing runtime-egress learning note and resolved runtime-proxy error were completed with September 26 revalidation and indexed, published as learn-notes `0e135f3` and error-notes `dc6a0a6`. All three pushes succeeded. Unrelated staged/unstaged changes were preserved. No application commit, build, test, deployment or crawl was performed during this documentation-only hook.

The earlier rejection and prepared entries below retain historical context; they do not describe the current Docker-note publication state. The evidence-loss fix still requires independent review and live acceptance, so no new resolved-error note or harness rule was created. Older source-specific draft entries below were not all published by this task.

## September 26 saved continuation

The user again requested save and stop. The current project checkpoint/roadmap are updated, and the coordinated source task stopped its agents. See `2026-09-26-paused-docker-checkpoint.md` and `2026-09-26-paused-evidence-preservation-fix.md` for the actual evidence and exact pending work. No new external note mutation/publication was attempted; the previously reported automatic-approval rejection remains recorded below.

For the next authorized devlog checkpoint/roadmap update: the previously paused API build/deployment completed, assets/warmup passed, 2,646 native tests passed before the final draft, and 22 frontend tests/type/lint passed. Real HTTP checks confirmed count-overflow handling, all 27 retired enhancements excluded, faction/detachment disclosure and real paid/free/unknown equipment pricing. Browser import displayed the incomplete-validation badge correctly. Three identity chats passed, but the combined comparison failed by discarding earlier successful points evidence after a later unresolved alias lookup. Its saved fix passed 56 focused tests but is unreviewed and undeployed; old E2E was 7/8 and its draft replacement remains unrun, with a mismatched mocked request route noted in the checkpoint. These are saved changes, not a release or real commit explanation. Preserve the September 22 runtime-proxy notes rather than duplicating them.

Potential reusable learning record after verified resolution: `date: 2026-09-26`, `project: wh40k-oracle`, `status: verified`, `promoted_to: []`, category `decisions/`. A later tool miss must not erase earlier substantive facts; an ID mapping alone does not establish rules or prices. Preserve historical authority boundaries while allowing supported partial answers. Deterministic unit coverage passed, but final live behavior and independent review must finish before labeling this a reusable success. Do not create a placeholder learning note now.

Potential error record after verified resolution: one underlying issue for successful evidence being discarded by automatic fallback. Reproduction is the exact saved Calgar/Guilliman comparison and trace in `2026-09-26-live-comparison.json`: successful `calc_points`, historical `get_datasheet`, empty composite-name resolver, automatic `rag_search`, then the incorrect claim that Guilliman current points were unavailable. Environment is local Windows/Docker Python API using the existing September 14 official snapshot. The positive scripted regression failed before the guarded continuation; paired tests passed 2/2 and the focused suite passed 56. Final resolution is **not yet verified live**. On resume, retain the failed response, add the fixed response under a new name, record the review/test results, and only then write the completed record using `error-notes/error-note-template.md` plus its index. No credentials or invented application commit should be included.

The ad-hoc acceptance script initially read `meta.degraded`, which is absent in this API. Manual inspection correctly found top-level `degraded=true`; future acceptance must assert the actual response schema and numeric/provenance behavior. This was caught during testing and is recorded as a project-specific verification correction, not promoted into a new global rule.

The project-local roadmap and acceptance report contain the complete current handoff. The earlier automatic approval review rejected a separate devlog write/publish action with only `blocked by policy`; this document prepares the concrete content without retrying that external mutation. No new cross-project harness rule is warranted.

## Project devlog entry

Intended destination: `D:/Project/devlog/wh40k-oracle/commits/20260920-b7dde5bdc-web-provenance-export.md`, with a linked current section in `wh40k-oracle/ROADMAP.md`.

Real implementation commits: `b7dde5bdc5c1451fc5d473d27e8c678d23402414` (web card citations and Markdown export), `ea84ff1bf` (named-table retention), `da57a429c` (line endings) and `d38a2fe7a` (copy panel). PR #74 carries the earlier canonical identity, faction inventory and lookup optimization work.

Why: a merged card lacked a dedicated web citation, and its scope warning was truncated; the layout model could then attach a general PDF page to card-specific counts and ranges. Add the explicit card source and retain scope first. Valid JSON also failed to guarantee that named rule-table rows survived into visible content; keep the original prose with a warning when they disappear. Provide a user-controlled, explicitly unreviewed Markdown snapshot instead of injecting AI text into the authoritative wiki.

Checks: 2,532 full native tests passed before the last two guard tests; final 40 focused tests passed. Two export tests, TypeScript, ESLint and production build passed. Rebuilt Docker services passed asset/warmup, codex, points, simulator and roster checks. Final command question returned all six effects with separate card/PDF citations, HTTP 200 in 55.215 seconds, no degradation. The browser copy panel delivered 4,551 characters with the AI label, source and full snapshot. Automatic download completion was not confirmed. The full September 18 benchmark remains 115/115; it was not rerun on September 20.

Remaining: public official rules for two units and 28 Ork enhancement keys; Docker restart durability; cloud deferred by user decision. No source refresh or database change in this pass. See the project acceptance report for final PR/merge status rather than inventing a merge commit here.

## Learning-note addition

Intended destination: append to `C:/Users/Administrator/learn-notes/decisions/20260918-inventory-counts-need-identity-semantics.md`, retaining its existing frontmatter (`date`, `project`, `status`, `promoted_to`).

### September 20 follow-through: check the final model boundary

The second LLM that formats an answer needs the same provenance boundaries as the reasoning agent. A correct source-scope warning on the first tool wire did not help when a later digest truncated it and offered only unrelated page citations. Preserve identity and scope before bulk text, and give each evidence class its own usable citation.

Also distinguish parseable JSON from complete user-visible content. In a controlled replay, the revised layout prompt put the full rule list under `verdict.calc` instead of top-level `calc`. Prompting alone was insufficient. A narrow deterministic named-table guard now preserves the full original reply with a warning when formatting loses rows. Do not describe this as general semantic validation; paraphrases and arbitrary sentence provenance still need separate evaluation. Before/after observations and failed responses remain in the project report.

## Resolved error record

Intended destination: `C:/Users/Administrator/error-notes/rag/20260920-error-01-web-card-citation-lost.md`, plus one README index link.

**Date:** 2026-09-20
**Project:** sha156/wh40k-oracle
**Status:** missing structured-card citation and scope truncation fixed; general model citation accuracy is not guaranteed.

### Error

No exception occurred in production. The initial HTTP response cited the general orders PDF page for the Leman Russ card's two-order/12-inch fields. That page supported general order effects and the ordinary 6-inch rule, not those card-specific numbers. The regression test looking for the missing L3 citation initially raised:

```text
StopIteration
next(c for c in answer.cites if c.book == "L3 结构库 · Alpha")
```

### Environment and reproduction

Windows, local Docker Desktop, Python backend/Next.js frontend; project runtime `D:/Project/py/RAG`. Return an official-db merged WikiPage from `get_entity`, including a long body and its `source_scope`, then format an answer with an independently retrieved general-rule PDF source. Before the fix, web `_derive_cites` omitted the merged card and `_evidence_digest` truncated the warning.

### Attempts and resolution

Agent-level scope disclosure alone was insufficient: the final web formatting boundary still lost it. Commit `b7dde5bdc` derives a dedicated L3 merged-card citation without inventing a page and puts scope ahead of bulk content. Regression tests confirm the card and real PDF citation coexist, unrelated patch-page references are not promoted and scope survives the digest limit. The final live HTTP answer cites the card-specific fields to L3 and the six general effects to PDF page 1. Failed lookup results do not create card citations.

```powershell
& 'D:/Project/py/RAG/.venv/Scripts/python.exe' -m pytest tests/test_web_card_provenance.py tests/test_web_api_stage3.py tests/test_agent_lookup_completeness.py -q
```

Final result: 40 passed. Do not infer that the separate named-table guard or one passing web response proves every future model citation correct.

### Lesson

Validate provenance after every transformation, especially a second formatting model. A source listed on a merged entity is not automatically provenance for every field. Keep failed evidence and distinguish deterministic source availability from probabilistic model selection.

## September 21 prepared continuation

Existing external notes were checked before preparing this continuation. No new external devlog, learning-note, error-note or harness mutation was performed. Publication remains blocked by the earlier automatic approval review (`blocked by policy`); the user subsequently requested save and stop. The current project-local handoff is `2026-09-21-paused-chat-calgar-checkpoint.md`.

### Project devlog and commit explanations

Intended project destination: `D:/Project/devlog/wh40k-oracle`, with real-commit explanations under `commits/` and the open work reflected in its checkpoint/roadmap.

- `793b7128b`: resolve the verified Guilliman short alias, preserve ambiguous lookup recovery and repeated-tool provenance, and disclose invalid simulator input instead of returning HTTP 500. Full native suite at that stage: 2,545 passed. The alias projection added one alias after a copied-DB trial; point values were unchanged. Live Guilliman returned 355 in 6.703 seconds without fallback; the comparison retained 355/100 and separate sources.
- `74a1bbf40`: explicitly select affordable `deepseek-flash` and disable hidden thinking in the relevant model clients. Direct provider response identified Flash; a constrained default-thinking call returned no visible text, while disabled thinking returned nonempty text. No Pro call was made. Flash-focused suite: 64 passed.
- `641d95b67a2efdd3514399d3e7561c153bbc8f75`: first complete conversational presentation. The user rejected its light theme and repetitive answer style. Later **uncommitted** revisions restore the original Warhammer appearance, group explanations and separate readable copying from archival export. Their frontend checks passed 10 tests, type/lint/build; desktop/copy checks passed. Do not attribute the uncommitted final appearance to this commit or call the first candidate accepted.

Remaining: finish historical Calgar tool/citation integration and repair its draft fixture; project only after copied-DB validation, deploy and verify; inspect final dark mobile layout; run final-tree review/tests and PR #75 checks. The archive worker's 37 tests pass, but the integration draft is 9 failed / 1 passed. Public Ork rules, Docker restart durability and download delivery limits remain unchanged.

### Reusable finding for learning notes

Intended addition to `C:/Users/Administrator/learn-notes/decisions/20260918-inventory-counts-need-identity-semantics.md`, retaining its template/frontmatter and `promoted_to` status.

An overlay importer is not a source archive. Black Library had ordinary Calgar's deleted historical record, but projecting Chinese details only onto canonical unit IDs silently excluded it from queryable storage. The verified cause is distinct from an active-unit filter: ordinary Calgar never existed in either current SQLite unit table. Preserve source identity, deletion status and raw payload separately; historical prices must not inherit current official authority. A fuzzy match to a differently equipped current unit is not a substitute. Source cache filesystem timestamps are not publication dates. These findings are verified; the proposed end-to-end archive fix is still incomplete and must not be recorded as a completed success.

### Resolved Guilliman error note draft

Intended destination: `C:/Users/Administrator/error-notes/rag/20260921-error-01-guilliman-short-name-lookup.md`, plus its README index entry. Date: 2026-09-21. Project: wh40k-oracle. Status: resolved for the verified short-name case.

Error shown to the user (no exception):

```text
get_datasheet('基里曼') → 库中未找到该单位
库中未找到基里曼的单位数据卡（get_datasheet 空结果），无法给出当前分数。
```

Reproduction/environment: local Windows/Docker app, query `基里曼当前多少分？`. The short resolver name was ambiguous with Ahriman, while full Chinese/English Guilliman names already found the structured 355-point entry. Datasheet lookup discarded recoverable ambiguity and the fallback returned unrelated historical PDF snippets without usable current points.

Attempts and verified resolution: compare short and canonical names first; adding a stronger LLM was not the underlying fix. Commit `793b7128b` adds the verified community alias and preserves ambiguity candidates for recovery. Trial the normal alias projection on a copied database, then apply the single added alias with a backup; no prices changed. Regression cases failed before the fix, the native suite at that stage passed 2,545, and the final live short-name response returned 355 with provenance and no fallback. The number is from the September 14 official snapshot, not a new September 21 fetch.

Lesson: separate name-resolution failures from missing source data and model availability. Keep uncertainty recoverable without loosening numeric fuzzy-match safeguards. Calgar is a separate source-retention problem and does not belong in this resolved issue record; the September 22 continuation below supersedes its earlier unresolved state.

## September 22 saved continuation

The user requested save and stop. The project checkpoint and roadmap are updated; no new commit, external note write or publication was performed by this source task. The earlier external mutation rejection remains in force. The coordinated Docker task owns its TLS/proxy note duplicate check and exact deployment report; consult its outcome before creating a duplicate. No cross-project harness promotion is warranted from this paused pass.

Stop-hook verification subsequently found three files already created by the coordinated Docker task before it received the instruction to pause external mutations: `D:/Project/devlog/wh40k-oracle/CHECKPOINT.md`, `C:/Users/Administrator/learn-notes/wins/20260922-test-container-egress-beyond-healthchecks.md`, and `C:/Users/Administrator/error-notes/rag/20260922-error-01-docker-llm-runtime-proxy.md`. All three are untracked local files; the learning note has the required frontmatter and the error note contains the redacted exception, reproduction, attempted fixes, verified recovery and limits. Do not duplicate or overwrite them. External roadmap/index additions and publication are still pending. This verification made no external changes, did not resume application work, and did not create a new commit explanation.

Project evidence: `2026-09-22-paused-review-crawl-checkpoint.md`, `2026-09-22-docker-acceptance.md`, `2026-09-22-archive-projection.json` and the saved partial source snapshot. Source changes remain uncommitted, so no new real-commit explanation exists yet. After committing, an explanation under `D:/Project/devlog/wh40k-oracle/commits/` must name that actual commit and distinguish it from `641d95b67`'s rejected first visual candidate.

### Update for the existing identity/coverage learning note

Intended destination remains `learn-notes/decisions/20260918-inventory-counts-need-identity-semantics.md`; preserve its template, unrelated content and promotion metadata. Verified extension: all 1,171 source inventory rows do not imply 1,171 usable details. The source's English-name detail endpoint returns a different faction's identity for four entries and cannot address 94 entries without English names. Retain the rejected raw response, compare source ID/faction before acceptance, and keep those gaps separate from two explicit empty detail responses. Across 38 faction/subfaction names, catalog-only factions also require enumeration. Snapshot hashes validate saved bytes; they do not establish complete rules coverage or official authority. Fresh Black Library Armour Calgar 140 differs from official-snapshot 155, while explicitly deleted ordinary Calgar remains historical 200.

Suggested frontmatter for a new record only if the existing note cannot accommodate the finding: `date: 2026-09-22`, `project: wh40k-oracle`, `status: verified`, `promoted_to: []`. Do not duplicate the earlier overlay-versus-archive finding.

### Resolved source-retention error draft

Intended destination: `error-notes/rag/20260922-error-01-deleted-source-identity-lost.md`, plus a single README index link after duplicate search. Date: 2026-09-22. Environment: Windows, project-native Python and local Docker. Status: historical lookup verified on the earlier deployed September 22 snapshot; final reviewed source API build remains pending.

Redacted error: ordinary Calgar returned no structured datasheet, or a fuzzy Armour of Antilochus candidate, despite the source cache containing `马涅乌斯.卡尔加（已删除）` with historical 200 points. Reproduce by querying ordinary Calgar's English/Chinese names when the canonical database contains only the differently equipped current variant. The overlay importer only projected matched canonical IDs; an unmatched deleted source record had no queryable storage. Changing the LLM or broadening fuzzy numeric matching would not repair that identity loss.

Resolution: retain explicit deleted records in an independent archive, preserve it through atomic DB rebuilds, expose exact verified aliases to agent tools, and supply dedicated historical citations. A copied-DB trial and live projection preserved all 17 preexisting tables and 1,721 canonical units. Live ordinary Calgar returned historical 200; Armour and Guilliman returned official-snapshot 155/355, without retrieval fallback. Five later regressions prove that a newly available exact official-ledger price takes precedence over archived price even without a canonical card; all failed before the correction and the focused suite passed 133. These final precedence guards await deployment. Lesson: preserve source identity independently from the canonical overlay and enforce authority at lookup boundaries, not solely in the model prompt.

### Additional verified regressions for the project devlog

Malformed model `final.sources` values previously caused formatting exceptions. Source-shape validation and bounded ASCII page parsing preserve usable replies and genuine card citations; targeted tests cover objects, integers, non-record list entries and Unicode digits. Unknown roster equipment previously used `.get(name, 0)` and silently skipped paid-option charges; six regression cases failed before the correction, then the 69-test roster suite passed. Verified free weapons remain supported and unresolved equipment is visibly unpriced. These are saved source fixes with final deployed acceptance pending, not completed-release claims.

Frontend stale-request fixes passed 22 unit tests, type/lint/build and selected live rapid-change journeys. The complete native suite passed 2,628 before the final focused additions, so the next release still needs a final-tree full run. Source capture remains partial, with all known inventory retained; no new raw snapshot was imported over the canonical cache or official points.
