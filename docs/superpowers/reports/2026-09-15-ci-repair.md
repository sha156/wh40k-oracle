# No-database CI repair

Date: 2026-09-15. Fix commit: `8a1687b2d`.

## Failure and change

Both push and pull-request jobs stopped during collection at `tests/test_ingest_vector_reuse.py:1` with `ModuleNotFoundError: No module named 'langchain_core'`. The new test imports ingestion code, while CI installed only server dependencies.

`requirements-ci.txt` now declares the tested LangChain/FAISS versions alongside server/test dependencies. CI also runs the formerly excluded PDF-ingestion and Markdown-chunking tests; those tests use synthetic data and require no model downloads. Full Streamlit application tests remain outside this dependency profile. Two inline application tests now explicitly check for Streamlit before importing the full application.

A tracked-files-only checkout exposed four more failures hidden by the collection error. Two roster tests called the default production database: they now use temporary schema-backed SQLite fixtures, with separate missing-database cases. Two real-CSV audits expected an ignored source cache: they now skip only when the cache directory is absent. An existing but empty/incomplete cache still fails its assertions; synthetic CSV parsing coverage always runs.

## Local verification

- Exported committed files to a separate temporary directory. Confirmed the production SQLite database and model directory were absent; production assets were never moved or modified.
- No-database export with the supported full project interpreter: **2,186 passed, 325 skipped**, 15 warnings.
- Focused tests with project assets: **93 passed**, including both real CSV audits and the added missing-database cases. The two full-app rules-floor tests additionally passed.
- Fresh Python 3.11 environment installed `requirements-ci.txt`; `pip check` passed and torch, sentence-transformers and Streamlit were absent. Focused ingestion/roster checks: **27 passed, 2 skipped** (the expected full-app cases).
- A full fresh Windows/Python 3.11 run exposed existing SQLite atomic-replace `WinError 32` failures in temporary database builds. This environment is not the supported local Python 3.9 or CI Linux/Python 3.10 profile. Those Windows failures are unresolved and are not claimed as passing.
- `git diff --check` passed. No production application code or data changed in this CI repair.

## Hosted acceptance

Both GitHub workflows passed for fix commit `8a1687b2d`:

- [Pull request run 34964046984](https://github.com/sha156/wh40k-oracle/actions/runs/34964046984): **2,180 passed, 331 skipped**, 10 warnings; frontend TypeScript, ESLint and static export all passed.
- [Push run 34964043179](https://github.com/sha156/wh40k-oracle/actions/runs/34964043179): both Python and frontend jobs passed.

The hosted skip count includes six more full-app tests than the full-dependency local export. The vector-reuse, PDF-ingestion and Markdown-chunking tests execute in hosted CI. Merge follow-through must verify the final PR head again after documentation commits; the external project devlog records the resulting merge.

## Remaining scope

The source reconciliation's released-Ork-text gaps, provider-balance blocker and Docker/WSL blocker remain unchanged. CI without local assets cannot certify those acceptance items.
